server:
  port: 8081
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/mongo_db
  application:
    name: product-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    enabled: true
    fetch-registry: true