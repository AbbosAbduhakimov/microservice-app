server:
  port: 8761
eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
spring:
  application:
    name: eureka

username: ${EUREKA_USERNAME:eureka}
password: ${EUREKA_PASSWORD:password}