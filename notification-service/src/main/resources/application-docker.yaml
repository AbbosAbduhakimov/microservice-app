spring:
  kafka:
    producer:
      bootstrap-servers:
        - localhost:9092
  cloud:
    eureka:
      client:
        service-url:
          defaultZzone: http://localhost:8761/eureka
  zipkin:
    base-url: http://localhost:9411
server:
  port: 8086
